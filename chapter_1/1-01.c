#include <stdio.h>

// Prints "hello, world"

main() {
    printf("hello, world\n");
}


// Observations

// Strings cannot be split into multiple lines.
// To break a line, we must use the /n character,
// which is also called an escape sequence.